version: 2

sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "./migrations"
    gen:
      go:
        emit_json_tags: true
        out: "./internal/store/pgstore"
        package: "pgstore"
        sql_package: "github.com/jackc/pgx/v4"
